# Environment Configuration
# Copy this file to .env and fill in your actual values

# Application Environment
NODE_ENV=development

# Server Configuration
HOST=127.0.0.1
PORT=5000

# Session Configuration
# Generate a secure random string for production using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-secure-session-secret-here

# Database Configuration
# For local PostgreSQL:
DATABASE_URL=postgresql://postgres:example@localhost:5432/safetycircle
# For Neon Database:
# DATABASE_URL=your_neon_database_url_here

# VAPID Keys for Push Notifications
# Generate these using: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your_vapid_public_key_here
VAPID_PRIVATE_KEY=your_vapid_private_key_here
VAPID_EMAIL=mailto:your-email@example.com
